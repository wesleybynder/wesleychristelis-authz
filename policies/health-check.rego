package system.health

# This rule defines the health check status of the system.
# By default, it returns true, indicating that the system is healthy.
# You can add more complex health checks as needed.

default healthy = true